using System;
using System.Collections.Generic;
using System.Data.Common;
using Swsu.BattleFieldMonitor.Models.Implementations;

namespace Swsu.BattleFieldMonitor.DataAccess
{
	internal static class ObstacleQueries
	{
		#region Methods
		/*public static IEnumerable<> SelectAll(DbConnection connection)
		{
			using (var command = connection.CreateCommand())
			{
				command.CommandText = "SELECT id, display_name, x, y, heading, speed FROM nkb_vs.unmanned_vehicle";

				foreach (var r in GetResults(command))
				{
					yield return r;
				}
			}
		}*/
		

		#endregion
	}
}